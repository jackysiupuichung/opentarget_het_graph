# Knowledge Graph Output
output/

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
env/
venv/
.env

# OS / editor
.DS_Store
.vscode/

# Data directories (root level only, not src/data)
/data/

# THBKG paper repo (tracked in its own GitHub repo) and local backups
THBKG/
THBKG.bak.*/

# SLURM job stdout dumps (sbatch writes <jobname>.o<jobid> into the cwd)
*.o[0-9]*

# Stray run logs in repo root (wandb sync, edge previews, etc.)
/*.log

# Retired off-scope eval script (target-discovery task dropped from the benchmark)
/evaluate_target_discovery_standalone.py
